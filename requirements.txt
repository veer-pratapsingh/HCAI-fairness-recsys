# Core data
pandas>=2.0
numpy>=1.24
pyarrow>=14.0

# Classic ML / recommenders
scikit-learn>=1.3
implicit>=0.7          # ALS collaborative filtering (LightFM does not build on modern Python)

# Deep learning (SASRec). Install the CUDA build matching your GPU from pytorch.org if needed.
torch>=2.1

# Stats (McNemar exact/continuity test + bootstrap)
scipy>=1.11

# Plotting
matplotlib>=3.7
seaborn>=0.12          # used by the Streamlit dashboard

# LLM recommender backends
openai>=1.0            # ChatGPT + NVIDIA NIM + Mistral (OpenAI-compatible endpoints)
google-generativeai>=0.5   # Gemini

# Dashboard
streamlit>=1.30
plotly>=5.18

# Config / utils
pyyaml>=6.0
tqdm>=4.65